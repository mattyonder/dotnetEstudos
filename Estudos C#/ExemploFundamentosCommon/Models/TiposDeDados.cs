using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Estudos_C_.Common.Models
{
    public class TiposDeDado
    {
        /*string @string = "Série de caracters";
        char @char = 'c';
        object? @object;
        bool @bool = true;
        byte @byte = 255;
        decimal @decimal = 3.4M;
        double @double = 3.44;
        int @int = 3;
        float @float = 3.4F;
        long @long = 3L;
        uint @uint = 3;
        short @shor = 3;
        ulong @ulong = 3;

        uint e ulong não representam valores negativos
        mas representam o dobro de positivos comparado a 
        suas contrapartes

         Decimal aconselhavel para representar dinheiro R$x,xx
         
         Tive que comentar tudo, pois no dotnet run estava aparecendo que as variaveis
         declaradas acima não estavam sendo usadas*/
    }
}